/var/log/preheat.log {
    weekly
    rotate 4
    compress
    delaycompress
    missingok
    notifempty
    create 0640 root root
    postrotate
        if [ -f /var/run/preheat.pid ]; then
            kill -HUP $(cat /var/run/preheat.pid) 2>/dev/null || true
        fi
    endscript
}
